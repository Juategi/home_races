class Comment{
  int id, competitionid, parentid, numanswers;
  String userid, comment, ip, iplocalization, image;
  DateTime date;
  Comment({this.id, this.image, this.numanswers, this.iplocalization, this.userid, this.competitionid, this.date, this.ip, this.comment, this.parentid});
}