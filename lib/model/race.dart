class RaceData{
  int id, time, steps, distance;
  String userid, image, firstname, lastname;
  Map<int, int> partials;
  RaceData({this.id,this.distance,this.userid,this.time,this.partials,this.steps, this.firstname, this.lastname, this.image});
}